# Optional ML dependencies for real emotion detection
# Install into backend venv:
#   cd backend
#   venv\Scripts\activate
#   python -m pip install -r requirements-ml.txt
#
# Note: TensorFlow wheels are large and may take time to install.

# Keep these compatible with Python 3.10 on Windows.
pillow>=10.4.0
opencv-python>=4.8.0
numpy>=1.21.0,<1.24.0
# TensorFlow 2.10.x requires protobuf <3.20.
protobuf==3.19.6
# Pin TF to avoid surprise major upgrades and reduce install churn.
tensorflow==2.10.1
